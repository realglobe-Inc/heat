---
適用: 常時
---

# HEAT: AI向け開発ガイドライン（プロジェクト固有）

この文書は、本リポジトリに貢献するAIアシスタント向けの実行時指示です。明示的なユーザー要求を最優先とし、この文書にない仕様は既存コードとテストから判断してください。対応バージョンとツール設定は `pyproject.toml`、公開されている利用方法は `README.md`、実行時の振る舞いはコードとテストを真実のソースとします。

## 1) 変更の基本方針

- 変更は要求を満たす最小限の範囲に限り、目的に無関係なリファクタ、整形、命名変更を混ぜない。
- 明示的に変更対象とされた仕様は、要求がない限り旧仕様との後方互換性を維持しなくてよい。変更対象でない外部仕様は維持する。
- `heat.HEAT` および `heat.model.HEAT` は公開APIとして扱う。コンストラクタ、`load_checkpoint()`、`infer()`、`infer_batch()` の引数、戻り値、例外、デバイス選択、チェックポイント互換性を破壊的に変更する場合は、要求と影響範囲を確認し、参照元、テスト、READMEを同時に更新する。
- 既存のモジュール境界、命名、ファイル配置に合わせる。行数だけを理由に機械的に分割せず、責務、依存方向、凝集度、テスト容易性を考慮する。一方で、既に大きいモデル実装へ無関係な責務を追加しない。
- 新規依存を追加する前に、標準ライブラリと導入済み依存で実現できないか確認する。追加が必要な場合は通常依存、開発依存、ビルド依存を区別し、`pyproject.toml` と `poetry.lock` を整合させる。
- 質問への回答や調査だけを求められた場合は、コードを永続的に変更しない。確認用の一時変更や生成物は作業終了時に除去する。
- `src/heat/models/ops/` にある第三者由来コードの著作権、ライセンス、出典コメントは保持する。

## 2) コードスタイルとドキュメンテーション

- Pythonバージョンは `pyproject.toml` の `[project].requires-python` を正とし、Python 3.14を前提に記述する。`# -*- coding:utf-8 -*-` は追加しない。
- 新規または変更する公開関数・メソッドには型ヒントを付ける。`Union` ではなく `|`、`Optional` ではなく `| None` を使う。依頼と無関係な既存コードの型付けを一括で行わない。
- Python 3.14ではannotationが遅延評価されるため、新規ファイルへ `from __future__ import annotations` を追加しない。既存ファイルからの削除だけを目的とした変更も行わない。
- フォーマットとLintは `ruff` に従う。相対インポートは禁止する（`ban-relative-imports = "all"`）。インポートは原則ファイル先頭に置くが、任意依存、ビルド時依存、循環参照の回避に必要なローカルインポートは認める。
- 新規または変更する公開APIのdocstring、および追加・変更するモジュールやクラスのdocstringはreStructuredText形式にする。依頼と無関係な既存docstringの形式や言語は変更しない。Google/NumPy形式は新たに導入しない。

  ```python
  def example(a: int, b: int) -> int:
      """
      1行要約。

      詳細な説明が必要なら段落で追記する。

      :param a: 入力 a の説明
      :param b: 入力 b の説明
      :returns: 戻り値の説明
      :raises ValueError: エラーとなる条件の説明
      """
      ...
  ```

- コメントは、非自明な制約、判断理由、テンソル形状、アルゴリズムの前提を残す場合に追加する。コードの処理をそのまま言い換えるコメントは追加しない。
- ログや `print` の追加は必要最小限にし、ライブラリ利用者の標準出力を意図せず変更しない。

## 3) プロジェクト構造と責務

- Pythonパッケージは `src/heat/` に配置する。このリポジトリにCLIエントリはないため、他プロジェクトのCLI構成を持ち込まない。
- `src/heat/model.py`: 公開クラス `HEAT`。モデルの構築、CPU/GPU選択、チェックポイント読み込み、入力検証、単一・バッチ推論を統括する。
- `src/heat/infer.py`: コーナー・エッジ推論と後処理。バッチ処理を変更する場合は、単一画像との結果整合性、空の検出結果、画像ごとの候補数の違いを考慮する。
- `src/heat/models/`: ResNet、コーナー・エッジモデル、Deformable Transformerなどのニューラルネットワーク実装。
- `src/heat/models/ops/`: Multi-Scale Deformable AttentionのC++/CUDA拡張、PyTorchフォールバック、ビルド処理。`setup.py` は `ops/build.py` をパッケージのimportなしで読み込むため、このビルド境界を維持する。
- `src/heat/datasets/` と `src/heat/utils/`: 画素特徴、テンソル、モデル共通の補助処理。モデル固有の制御フローを安易に移さない。
- 公開推論APIは、チェックポイント読み込み後に、チェックポイントから決まる `image_size`（未指定時は256）と一致するBGR順の `np.uint8` 画像を受け取る。意図した仕様変更でない限り、この入力契約とコーナー・エッジ配列の意味を維持する。

## 4) チェックポイント、CUDA、ビルド

- `load_checkpoint()` は `torch.load(..., weights_only=False)` を使用するため、プロジェクトが指定した配布元、またはユーザーが信頼を確認したチェックポイントだけを読み込む。出所不明の `.pth` をテストや調査で実行しない。
- 学習済みチェックポイントは大容量かつ環境依存のテスト資産として扱い、リポジトリへコミットしない。認証情報や機密情報もコード、ログ、テストデータ、チェックポイントへ記録しない。
- CUDA拡張は、CUDAツールキットが検出され、CUDAが利用可能か `TORCH_CUDA_ARCH_LIST` が指定された場合にビルドされる。CPU環境や拡張未導入環境での推論はPyTorch実装へフォールバックする。
- PyTorchフォールバックのforwardと、CUDA拡張を必要とするbackwardを混同しない。学習・backwardの変更は、推論のみのテストで検証済みと判断しない。
- CPUだけの環境で検証可能な変更は `force_cpu=True` を使ってテストする。CUDA固有の変更をGPU/CUDA環境で検証できない場合は、未検証の範囲を最終報告に明記する。
- CUDA/C++ソース、`setup.py`、`ops/build.py`、ビルド依存を変更した場合は、可能な環境で拡張のインプレースビルドまたはwheelビルドも確認する。生成された共有ライブラリ、`build/`、キャッシュはコミットしない。

## 5) テストと検証

- テストフレームワークはpytest。変更範囲に対応する軽量テストを先に実行し、必要に応じて統合テストへ広げる。
- `tests/test_api_validation.py` はチェックポイント不要の軽量テストである。公開APIの入力検証、CPU動作、候補生成を変更した場合は、まずこのテストを実行する。
- `tests/test_main.py` と `tests/test_batch_infer.py` は `integration` マーカー付きで、`test_data/test_image.png` と学習済みチェックポイントを使用する。`test_main.py` はプロジェクト指定の配布元からチェックポイントを取得する場合があるが、`test_batch_infer.py` 単独では取得しない。個別実行前に配置を確認し、無条件に軽量テストへ含めない。
- 推論結果や後処理、チェックポイント読み込み、単一・バッチ間の整合性を変更した場合は、利用可能なら統合テストも実行する。期待配列は、要求による意図した出力変更を反映する場合に限って更新する。
- コードを変更した場合は関連テストを必ず実行する。ドキュメントだけの変更で実行時の振る舞いに影響しない場合、pytestは必須としない。
- 変更と無関係に見える失敗は、可能な限り変更前でも再現することを確認する。修正対象外とする場合は、実行コマンド、失敗内容、未検証範囲を報告する。
- テストを削除、スキップ、弱体化して失敗を隠さない。テストを通すためだけに期待値を変更しない。

## 6) 実行コマンド

- 依存関係の導入: `poetry install`
- 変更したPythonファイルのフォーマット: `poetry run ruff format <changed files>`
- 変更したPythonファイルのLint: `poetry run ruff check <changed files>`
- 全体Lint: `poetry run ruff check .`
- 軽量テスト: `poetry run pytest -q -m "not integration"`
- 関連テスト: `poetry run pytest -q <test paths>`
- 統合テストのみ: `poetry run pytest -q -m integration`
- 全テスト: `poetry run pytest -q`
- CUDA拡張のインプレースビルド: `poetry run python setup.py build_ext --inplace`
- wheelビルド: `poetry build -f wheel`

## 7) 禁止事項

- 目的に無関係な大規模リファクタ、整形、命名一括変更。
- 公開API、チェックポイント形式、推論結果の意味の意図しない変更。
- テストの削除、無効化、弱体化、またはテストを通すためだけの期待値更新。
- 相対インポート、未使用依存、用途区分が誤った依存の追加。
- 信頼できないチェックポイントの読み込み、機密情報や大容量モデルファイルのコミット。
- `__pycache__/`、共有ライブラリ、`build/` など生成物のコミット。
